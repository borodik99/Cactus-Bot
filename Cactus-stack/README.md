# 🌵 CactusBot — Макс

> Telegram-бот для управления очередью полива офисного кактуса

![Node.js](https://img.shields.io/badge/node-%3E%3D18.0.0-green)
![Platform](https://img.shields.io/badge/platform-Telegram-blue)
![License](https://img.shields.io/badge/license-MIT-brightgreen)

Больше не нужно помнить, кто последний поливал Макса.  
Бот сам ведёт очередь, напоминает кому и когда поливать, хранит историю и позволяет пропустить очередь через запрос к админу.

---

## ✨ Возможности

🌿 **Очередь полива:**
- Участники вступают и выходят из очереди через кнопки
- После каждого полива очередь автоматически ротируется
- Текущий участник видит кнопку «Пропустить очередь»

💧 **История поливов:**
- Хранит дату, время и объём каждого полива
- Последние 10 записей доступны в меню

🔔 **Автоматические напоминания:**
- Уведомление каждые ~10 рабочих дней текущему участнику
- Ежедневная проверка — повтор если полив просрочен

⏩ **Пропуск очереди:**
- Участник отправляет причину пропуска
- Админ одобряет или отклоняет запрос
- При одобрении очередь автоматически передаётся следующему

✅ **Управление доступом:**
- Новые пользователи проходят одобрение у админа
- Админ может выдать или забрать доступ в любой момент

---

## 📸 Демо

```
📋 Очередь полива Макса:

💧 Максим ← сейчас
2. Роман
3. Pasha
4. Адриан
```

---

## 📁 Структура проекта

```
project/
├── index.js           # Точка входа, запуск бота
├── bot.js             # Инициализация бота и подключения к БД
├── constants.js       # Настройки полива (частота, объём и др.)
├── helpers/
│   └── index.js       # Утилиты (ensureApproved)
├── db/
│   └── queries.js     # Все запросы к базе данных
├── keyboards/
│   └── index.js       # Клавиатуры бота
├── handlers/
│   ├── commands.js    # /start, /menu, /help, /users, /approve, /revoke, /watered
│   ├── callbacks.js   # Обработчики inline-кнопок
│   └── admin.js       # Одобрение/отклонение пропуска очереди
└── cron/
    └── index.js       # Планировщик напоминаний
```

---

## 📥 Установка

**1. Клонируй репозиторий:**
```bash
git clone https://github.com/your/cactus-bot.git
cd cactus-bot
```

**2. Установи зависимости:**
```bash
npm install
```

**3. Создай файл `.env`:**
```env
BOT_API_KEY=your_telegram_bot_token
DATABASE_URL=postgres://user:password@host:5432/dbname
ADMIN_CHAT_ID=your_telegram_chat_id
```

**4. Инициализируй базу данных:**
```bash
psql -U user -d dbname -f init.sql
```

**5. Запусти бота:**
```bash
node index.js
```

---

## 🎮 Использование

1. Напиши боту `/start`
2. Дождись одобрения от админа
3. Нажми **«Участвовать в поливе»** — встанешь в конец очереди
4. Когда придёт твоя очередь — получишь уведомление с кнопкой **«Полил!»**
5. После полива очередь автоматически перейдёт к следующему

---

## 💬 Команды

| Команда | Описание |
|---|---|
| `/start` | Запустить бота |
| `/menu` | Главное меню |
| `/watered` | Отметить полив |
| `/help` | Список команд |
| `/users` | Все пользователи *(админ)* |
| `/approve ID` | Выдать доступ *(админ)* |
| `/revoke ID` | Забрать доступ *(админ)* |

---

## ⚙️ Как это работает?

**Логика очереди:**
```
Участник поливает
       ↓
rotateQueue() — поливший уходит в конец
       ↓
Следующий получает уведомление
```

**Пропуск очереди:**
```
Участник нажимает «Пропустить» → пишет причину
       ↓
Админ получает запрос с кнопками [✅ Передать] [❌ Отклонить]
       ↓
При одобрении — очередь сдвигается, следующий получает уведомление
```

**Файл конфигурации `.env`:**
```env
BOT_API_KEY      # токен от @BotFather
DATABASE_URL     # строка подключения к PostgreSQL
ADMIN_CHAT_ID    # твой Telegram ID (узнать у @userinfobot)
```

---

## 🛠️ Сборка для разработчиков

Понадобится Node.js 18+.

```bash
# Клонируй репозиторий
git clone https://github.com/your/cactus-bot.git
cd cactus-bot

# Установи зависимости
npm install

# Запусти в режиме разработки
node index.js
```

---

## 📦 Стек

| Пакет | Назначение |
|---|---|
| [grammY](https://grammy.dev) | Telegram Bot framework |
| [node-postgres](https://node-postgres.com) | PostgreSQL клиент |
| [node-cron](https://github.com/node-cron/node-cron) | Планировщик задач |
| [dotenv](https://github.com/motdotla/dotenv) | Переменные окружения |

---

## 🤝 Вклад в проект

Нашли баг или есть идея для фичи?
- Откройте [Issue](https://github.com/your/cactus-bot/issues)
- Сделайте Fork репозитория
- Отправьте Pull Request

---

> 🌵 Берегите Макса
